export interface ISeason {
  league: string;
  startDate: Date;
  endDate: Date;
  activeSeason: boolean;
}

export interface ISeasonData {
  league: string;
  start_date: Date;
  end_date: Date;
  active_season: boolean;
}
